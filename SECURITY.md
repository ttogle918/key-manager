<!--
SPDX-FileCopyrightText: 2026 [Your Name]
SPDX-License-Identifier: MIT
-->
# 보안 정책 (Security Policy)

KeyLens는 자격증명(API 키·토큰)을 다루는 도구이므로 보안 신고를 진지하게 받습니다.

## 취약점 신고

**공개 이슈로 올리지 마세요.** 대신 비공개로 알려주세요:

- GitHub **[Security Advisories](https://github.com/ttogle918/key-manager/security/advisories/new)** 의
  "Report a vulnerability"(비공개 취약점 보고)를 사용하는 것을 권장합니다.
- 재현 절차·영향 범위·가능하면 PoC를 포함해 주세요. **재현에 실제 키를 쓰지 말고 더미 값**을 쓰세요.

접수 후 확인 회신을 드리고, 수정과 공개(advisory) 시점을 함께 조율합니다.

## 범위

이 프로젝트는 **로컬 우선(local-first)** 도구입니다 — 백엔드는 `127.0.0.1` 에만 바인딩되고, 데이터는
사용자 기기의 암호화된 SQLite(`vault.db`, 암호문만)에만 존재합니다. 다음이 관심 대상입니다:

- 암호화·키 유도 결함(Argon2id/AES-256-GCM 사용, nonce 재사용, 평문 유출 등)
- 인증 게이트 우회(잠금 상태에서 값 노출, 자동 잠금·실패 지연 우회)
- 금고 파일/번들에서 평문·키 유출, 가져오기(import) 시 검증 우회
- 입력 처리로 인한 크래시·DoS(분류 파이프라인, OCR, 번들 파싱)
- 분류 오식별로 인한 잘못된 변수명 매핑(안전 측면)

### 위협 모델 · 범위 밖

상세 위협 모델과 방어 범위는 [SECURITY_REVIEW.md](./SECURITY_REVIEW.md) 참고. 요약하면 **범위 밖**:

- 이미 장악된 호스트(키로거·메모리 덤프·루트킷)
- 약한 마스터 비밀번호(사용자 책임 — 강도 힌트만 제공)
- 사용자가 명시적으로 트리거하는 외부 호출(TRUST-1 키 검증 — 사용자 소유 키를 소유 서비스로만)

## 지원 버전

정식 릴리스 전(pre-1.0)이라 **최신 `main`** 기준으로만 수정합니다.

## 시크릿 위생

이 저장소는 자격증명 도구입니다. **실제 키·시크릿을 이슈·PR·커밋·테스트에 절대 포함하지 마세요.**
모든 예시·픽스처는 명백한 더미(`sk-xxxxxxxx` 등)여야 합니다.
