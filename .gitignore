# --- 시크릿 & 자격증명 ---
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
service-account*.json
# 실제 키가 담긴 테스트 입력 이미지 등(로컬 전용, 커밋 금지). 데모용 더미는 docs/demo/ 사용.
# 루트 앵커(/) — frontend/src/data 같은 하위 data 디렉터리는 건드리지 않는다.
/data/

# --- 금고/앱 데이터 (실데이터 절대 커밋 금지) ---
*.sqlite
*.sqlite3
*.db
vault.db

# --- Python (FastAPI 백엔드) ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Node / React 프론트엔드 ---
node_modules/
dist/
build/
.vite/
*.local
npm-debug.log*

# --- OS / 에디터 ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- Claude Code (프로젝트 자산은 커밋, 머신 로컬 설정만 제외) ---
.claude/settings.local.json

# --- OCR 모델 캐시(용량 크고 재생성 가능) ---
*.traineddata
.cache/
# tesseract.js 로컬 벤더 자산 — scripts/vendor-tesseract.mjs 가 재생성(커밋 금지)
frontend/public/tesseract/
frontend/public/tessdata/
