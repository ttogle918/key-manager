# --- 시크릿 & 자격증명 ---
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
service-account*.json
# 실제 키가 담긴 테스트 입력 이미지 등(로컬 전용, 커밋 금지). 데모용 더미는 docs/demo/ 사용.
# 루트 앵커(/) — frontend/src/data 같은 하위 data 디렉터리는 건드리지 않는다.
/data/

# --- 금고/앱 데이터 (실데이터 절대 커밋 금지) ---
*.sqlite
*.sqlite3
*.db
vault.db
local_discoveries.yaml

# 금고 내보내기 번들(SYNC-0) — 암호문이지만 사용자 백업이라 커밋 금지.
*.klvault.json

# --- Python (FastAPI 백엔드) ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Node / React 프론트엔드 ---
node_modules/
dist/
build/
.vite/
*.local
npm-debug.log*

# --- OS / 에디터 ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- Claude Code (프로젝트 자산은 커밋, 머신 로컬 설정만 제외) ---
.claude/settings.local.json
# WIP 체크포인트(/checkpoint) — 세션 핸드오프용 임시 메모, 커밋 금지
docs/CHECKPOINT.md
# 세션별 작업 로그(개인 참고용 — 의논 맥락·의사결정 기록, 커밋 금지)
docs/session_log/

# --- Claude Design 프로토타입 export (로컬 참고용 — React 이식 완료, 커밋 금지) ---
KeyLens 자격증명 관리 도구/

# --- 대회 제출용 문서(개인 서명·접수번호 포함, 리포에 커밋 금지) ---
*.docx
*.hwp
*.hwpx
# 제출양식에 맞춰 채운 결과보고서 사본(개인정보 포함) — 원본 docs/RESULT_REPORT.md 는 커밋한다.
docs/RESULT_REPORT_제출양식.md
# 대회 포털에 낸 제출물(결과보고서 PDF/zip) - 용량이 크고 저장소에 둘 이유가 없다.
/2026 오픈소스 개발자대회 결과보고서*

# --- OCR 모델 캐시(용량 크고 재생성 가능) ---
*.traineddata
.cache/
frontend/public/tessdata/
# RapidOCR 한국어 인식 모델 — backend/scripts/vendor_ocr_models.py 가 재생성(커밋 금지)
backend/app/ocr_models/
