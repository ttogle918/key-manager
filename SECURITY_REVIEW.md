<!--
SPDX-FileCopyrightText: 2026 [Your Name]
SPDX-License-Identifier: MIT
-->

# SECURITY_REVIEW — KeyLens 보안 감사 (2026-07-05)

> 대상: 저장소 전체 (docs/SPEC.md · docs/BACKLOG.md · backend/ · frontend/ · 스크립트 · 저장소 위생)
> 목적: 방어 관점의 설계 결함·구현 오류 점검. 공격 기법 아님.
> 기준 시점: `main` 최신 커밋 + 작업 트리 수정분.
>
> **📌 갱신(2026-07-05):** 아래 총평·findings 는 **감사 시점 스냅샷**이다. 이후 최대 리스크였던
> 허위 보안 표시 해소 + **VAULT-1/2(암호화 저장·인증 게이트) 구현·브라우저 검증**이 완료됐다.
> 현재 조치 상태는 하단 **"우선순위 요약 및 조치 현황"** 표를 기준으로 본다.

## 총평

*(감사 시점 서술 — 현재 상태는 상단 갱신 노트·하단 조치표 참고)*

**이 프로젝트의 핵심 보안 기능(암호화 저장 VAULT-1, 인증 게이트 VAULT-2)은 아직 한 줄도 구현되지 않았다.** 이것 자체는 결함이 아니다 — BACKLOG 스프린트 계획(S3: 7/21–8/3)에 미구현으로 정직하게 기재되어 있고, 현재 프론트 볼트는 명시적 목업이다.

**진짜 문제는 산출물이 미구현 사실을 숨긴다는 것이다.** UI 토스트·헤더·README가 전부 현재형으로 "AES-256 암호화 완료", "디스크에는 암호문만 저장"이라고 주장하고, 잠금 화면은 아무 비밀번호나 통과시킨다. 지금 상태로 시연 영상이나 2차 기능 테스트에 노출되면 "허위 보안 표시"로 읽히며, 이는 보안 과제 프로젝트에서 기능 부재보다 치명적인 감점 요인이다. 반면 분류 엔진(백엔드)·OCR·시크릿 위생(.gitignore, 더미 값 규율)은 견실하다.

**심각도 집계**: 상 4건 · 중 9건 · 하 8건

| 점검 항목 | 판정 |
|---|---|
| 1. 설계 vs 구현 정합성 | ⚠️ 암호화·인증·설정 문서에서 광범위한 드리프트 |
| 2. 키 생명주기 | ❌ 저장 평문·영속성 없음 / 폐기·로테이션은 표시뿐 |
| 3. 접근 제어·감사 | ❌ 인증이 장식(아무 비밀번호 통과), 자동잠금·실패지연·감사로그 없음 |
| 4. 암호화 구현 | ➖ 구현 자체가 없어 평가 불가 (설계 선택은 적절) / 하드코딩 시크릿 없음 ✅ |
| 5. 장애·예외 대응 | ⚠️ 백엔드 장애 시 가짜 결과 폴백이 위험 / 에러 메시지 위생은 양호 ✅ |

---

## 1. 설계 문서 vs 실제 구현 정합성

### 1-1. [상] 미구현 암호화를 "구현된 것"으로 표시하는 허위 보안 문구
- **위치**:
  - `frontend/src/store/keylensStore.ts:445` — 저장 토스트 `'저장됨 · AES-256 암호화 완료'`
  - `frontend/src/components/screens/VaultScreen.tsx:42` — 헤더 `"N개 자격증명 · AES-256 로컬 암호화"`
  - `frontend/src/components/screens/SetupScreen.tsx:47` — "이 비밀번호로 암호화되어 이 기기에만 저장됩니다"
  - `frontend/src/components/screens/InputScreen.tsx:272` — "암호화되어 이 기기에만 보관됩니다"
  - `README.md:26` — 주요 기능에 현재형으로 "**암호화 보관**: … 디스크에는 암호문만 저장"
- **근거**: 실제 저장은 Zustand 메모리 배열이며 `VaultItem.full`에 평문 그대로 들어간다(`frontend/src/types.ts:70`). 암호화 코드(`crypto.py`, `vault_repo.py`)는 저장소 어디에도 없다. SPEC 4.6/6.2가 약속한 Argon2id·AES-256-GCM·"평문 값 컬럼 없음"은 전부 미래형인데 사용자 대면 문구는 전부 완료형이다.
- **왜 심각한가**: 사용자가 실제 키를 붙여넣고 "암호화 완료" 토스트를 믿게 만든다. 보안 도구에서 가장 나쁜 실패 모드는 "보호되지 않는데 보호된다고 믿게 하는 것"이다. 대회 2차 평가(다른 환경에서 직접 실행)에서 SQLite/저장 파일을 열어보는 순간 드러난다.
- **개선안**:
  1. VAULT-1 구현 전까지 모든 완료형 문구를 제거하거나 "(구현 예정)"으로 교체. 최소 변경: 토스트를 `'저장됨 (데모 — 암호화는 VAULT-1에서)'`로.
  2. 앱 셸에 "데모 모드 — 값이 암호화되지 않습니다" 배너를 상시 표시.
  3. VAULT-1 완료 시점에 문구를 복원하는 항목을 BACKLOG VAULT-1 체크리스트에 추가.

### 1-2. [중] README 요구사항·아키텍처가 실제 OCR 구현과 불일치
- **위치**: `README.md:49` (네이티브 Tesseract 설치 요구), README 아키텍처 다이어그램(백엔드에 "OCR (Tesseract)"), `docs/SPEC.md:132` (백엔드 OCR)
- **근거**: CORE-3 결정(BACKLOG)에 따라 OCR은 **브라우저 tesseract.js**로 구현됐다(`frontend/src/ocr/ocr.ts`). 백엔드에는 OCR 코드가 없고, 네이티브 Tesseract는 어디서도 사용되지 않는다. README대로 설치하면 불필요한 시스템 의존성을 깔게 되고, 반대로 실제 필요한 준비(`npm run` 벤더링 스크립트 등)는 안내되지 않는다.
- **개선안**: README 요구사항에서 네이티브 Tesseract 제거, 아키텍처 다이어그램을 "프론트엔드: OCR(tesseract.js, 로컬 WASM)"으로 수정. SPEC 5.1도 동일하게 갱신(재현성 평가 OSS-3와 직결).

### 1-3. [중] `.env.example`이 존재하지 않는 설정 계약을 문서화 — 실제 코드는 다른 변수를 읽음
- **위치**: `.env.example` 전체 vs `frontend/src/api/client.ts:7-11`, `backend/app/main.py:73`
- **근거**:
  - `.env.example`은 `VITE_API_BASE_URL=http://127.0.0.1:8000`을 안내하지만 코드가 읽는 변수는 **`VITE_API_BASE`**이고 기본 포트는 **8003**이다. 문서대로 설정하면 조용히 무시된다.
  - `SESSION_TIMEOUT_MINUTES`, `MAX_AUTH_ATTEMPTS`, `AUTH_LOCKOUT_SECONDS`, `DATABASE_PATH`, `OCR_ENGINE=paddleocr`, `TESSERACT_CMD_PATH` — 전부 읽는 코드가 없다. 특히 보안 파라미터 3종은 "설정하면 동작하는 것처럼" 보여 잘못된 안전감을 준다.
- **개선안**: `.env.example`을 실제 읽히는 변수만 남기고 정리(`VITE_API_BASE` 하나가 현재 전부). 미래 설정은 주석으로 "VAULT-2에서 도입 예정"임을 명시.

### 1-4. [중] 지식베이스(백엔드)와 프론트 TYPE_MAP 불일치 — Kakao Native 키가 매핑 불가
- **위치**: `backend/knowledge/kakao.yaml:39-47` (4종: rest/js/admin/**native**) vs `frontend/src/data/services.ts:20-24` (3종: rest/js/admin)
- **근거**: 백엔드가 `KAKAO_NATIVE_APP_KEY`로 분류해 반환하면 `map.ts:85`의 `TYPE_MAP[svc].find(...)`가 실패해 `typeKey=''`(종류 미확정)로 떨어지고 저장이 막힌다. SPEC 4.4 AC "YAML 한 개 추가만으로 분류 대상에 포함(코드 수정 0)"이 프론트 하드코딩 때문에 실제로는 성립하지 않는다(Ollama 추가 때도 프론트 서비스맵을 함께 수정해야 했음 — BACKLOG 기록).
- **개선안**: 단기 — `TYPE_MAP.Kakao`에 `{ v: 'native', label: 'Native 앱 키', var: 'KAKAO_NATIVE_APP_KEY' }` 추가. 근본 — INTEG-1 잔여 작업인 `GET /knowledge` 기반 동적 구성으로 하드코딩 제거(백로그에 이미 기재됨 — 우선순위 상향 권장).

### 1-5. [하] SPEC 4.2 "10종 키 식별" AC 미충족 (알려진 미결)
- **위치**: `docs/SPEC.md:86-87` vs `backend/knowledge/*.yaml` (실제 값 기반 식별: OpenAI 2종·GCP 1종·Notion 1종·Ollama 1종)
- **근거**: GitHub `ghp_`/AWS `AKIA`/Slack `xoxb-` 등은 KB에 없다. 다만 오분류는 없고(엔트로피 판정으로 `unknown` 처리, `stage1.py:50-58`), BACKLOG CORE-1에 "미결"로 정직하게 기재되어 있다.
- **개선안**: 결과보고서/SPEC의 AC 문구를 실제 범위("KB 등록 서비스")로 내리거나, 검출 전용 `value_rules`를 추가해 AC를 충족(추가 시 Gitleaks 참고분 출처 기록 규칙 준수).

---

## 2. 키 생명주기

### 2-0. 키 생성 — 해당 없음 (통과)
이 도구는 키를 생성하지 않고 제3자 발급 자격증명을 보관만 한다. CSPRNG 요구사항은 향후 VAULT-1의 **솔트·nonce 생성**에 적용된다(개선안 2-1에 포함). 데모 더미 값은 고정 상수이며(`docs/demo/generate.py:23-32`) 실키와 충돌하지 않는 명백한 가짜다 — 적절.

### 2-1. [상] 키 저장: 평문 메모리 + 영속성 전무, 새로고침 시 전체 소실
- **위치**: `frontend/src/store/keylensStore.ts:236` (`vault: seedVault()`), `frontend/src/types.ts:63-79` (`full: string` 평문)
- **근거**: 저장소 계층이 없다. "저장"된 키는 탭을 닫는 순간 사라지고, 다음 실행 시 시드 더미 9건으로 리셋된다. 기밀성(평문) 문제이자 가용성(소실) 문제다. localStorage 등에 **평문으로 영속화하지 않은 것**은 오히려 다행이다 — 어설픈 평문 영속화보다 현 상태가 낫다.
- **개선안**: VAULT-1을 계획대로 최우선 구현하되, 현재 아키텍처(값이 프론트에 존재)를 반영해 저장 위치를 명확히 결정할 것:
  - 프론트 저장 시: WebCrypto `PBKDF2/Argon2id(WASM) → AES-GCM`, IndexedDB에 암호문만.
  - 백엔드 저장 시(SPEC 원안): `cryptography` Argon2id + AES-256-GCM, SQLite 암호문만. 값이 loopback HTTP로 백엔드에 전달되는 흐름을 SPEC 5.2에 명시.
  - 어느 쪽이든 솔트·nonce는 CSPRNG(`crypto.getRandomValues` / `secrets`)로 항목당 고유 생성.

### 2-2. [상] 원본 스크린샷(평문 키가 찍힌 이미지)이 볼트에 저장되고, 잠금 상태에서도 그대로 표시됨
- **위치**: 저장 — `frontend/src/store/keylensStore.ts:180` (`sourceImage: get().analyzedImage`), 표시 — `frontend/src/components/vault/VaultRow.tsx:31-32, 116-125`
- **근거**: 스크린샷 data URL이 볼트 항목에 통째로 들어간다. 스크린샷에는 키 **값이 마스킹 없이** 찍혀 있다. 상세 펼침(`toggleExpanded`)과 이미지 렌더링(`hasRealImg`)은 `locked`를 확인하지 않으므로, **잠금 상태에서 값 필드는 마스킹돼도 스크린샷으로 원문을 읽을 수 있다** — 잠금 마스킹(SPEC 6.4)의 실질적 우회 경로다.
- **개선안**:
  1. 즉시: `VaultRow`에서 `locked || !justUnlocked && !revealed`면 스크린샷을 블러/placeholder 처리.
  2. VAULT-1에서: `sourceImage`도 값과 동일하게 암호화 대상에 포함(SPEC 6.2 "SQLite에는 암호문·nonce·메타데이터만"의 '메타데이터'에 스크린샷이 포함되지 않도록 명시).

### 2-3. [중] 키 로테이션: "회전 기록"이 실제 회전이 아님 — 값 교체 수단 자체가 없음
- **위치**: `frontend/src/store/keylensStore.ts:541-551` (`rotate` — history에 한 줄 추가뿐), `VaultRow.tsx` (값 편집 UI 없음)
- **근거**: 사용자가 서비스 콘솔에서 키를 재발급해도 KeyLens 안의 옛 값을 새 값으로 바꿀 방법이 없다(삭제 후 재등록만 가능). "회전 기록" 버튼은 이력만 남겨, 볼트의 값이 최신이라는 잘못된 믿음을 만든다. 폐기된 옛 키가 '현행'으로 계속 노출·복사되는 상태다.
- **개선안**: (a) 항목의 값 교체 플로우 추가(새 값 입력 → 이전 값은 이력에 마스킹 보관 또는 즉시 폐기), (b) 그 전까지는 버튼 라벨을 "회전 메모"로 바꾸고 "값은 삭제 후 재등록하세요" 안내를 붙일 것.

### 2-4. [중] 키 폐기: 삭제는 배열 제거뿐, 클립보드 자동 삭제는 허위 안내
- **위치**: `frontend/src/store/keylensStore.ts:554-559` (`confirmDelete`), `keylensStore.ts:525-536` (`copy` — 토스트 `'30초 후 클립보드에서 지워져요'`)
- **근거**:
  - 삭제: 메모리 배열 filter뿐이며, JS 문자열은 불변이라 zeroing이 불가능하다(GC 의존). 이는 언어 한계로 수용 가능하지만 SPEC "한계" 절에 명시가 없다.
  - **클립보드: 지우는 코드가 존재하지 않는데 30초 후 삭제된다고 안내한다.** 사용자가 이를 믿고 클립보드 위생을 스스로 챙기지 않게 된다. SPEC 6.5의 "짧은 시간 후 자동 비우기(선택)"가 문구만 먼저 들어온 상태.
  - `VaultItem` 원본이 `revealed`/재마스킹과 무관하게 메모리에 상주 — 장악된 호스트는 위협 모델 밖(SPEC 6.1)이므로 수용 가능.
- **개선안**: `copy()`에 `setTimeout(30s)` 후 `navigator.clipboard.writeText('')`(단, 사용자가 그 사이 다른 것을 복사했으면 건드리지 않도록 현재 클립보드 값 비교 — Firefox의 readText 미지원 폴백 포함)를 실제 구현하거나, 구현 전까지 토스트 문구 제거. 삭제의 zeroing 불가는 SPEC 10(한계)에 1줄 추가.

---

## 3. 접근 제어 및 권한

### 3-1. [상] 인증이 장식: 아무 비밀번호나 잠금 해제, 마스터 비밀번호는 검증조차 안 함
- **위치**: `frontend/src/store/keylensStore.ts:284-295` (`submitUnlock` — 빈 문자열만 거부, 검증 없이 750ms 후 해제), `keylensStore.ts:265-280` (`createVault` — 길이·일치 확인 후 비밀번호를 버림)
- **근거**: 설정한 마스터 비밀번호는 어디에도(해시조차) 저장되지 않고, 잠금 해제는 입력값과 무관하게 성공한다. 잠금 화면의 "비밀번호는 이 기기에서만 검증됩니다"(`LockScreen.tsx:73`)조차 사실이 아니다 — 검증 자체가 없다. VAULT-2 목업임은 백로그에 기재돼 있으나, 1-1과 결합하면 "암호화+인증이 다 있다"는 완전한 허상이 만들어진다.
- **개선안**: VAULT-1/2 구현 시 인증을 "비밀번호 → KDF → 복호화 성공 여부"로 정의(별도 비밀번호 해시 저장 불필요 — GCM 태그 실패 = 인증 실패). 그 전까지는 잠금 화면에 "데모: 인증 미구현" 표시. 시연 영상에 잠금 해제 장면을 넣는다면 반드시 구현 후에.

### 3-2. [중] 자동 잠금·인증 실패 지연 부재 (SPEC 6.4 명시 요구)
- **위치**: 부재 증명 — `keylensStore.ts` 전체에 잠금 타이머 없음(`lockNow`는 수동 전용), `submitUnlock`에 실패 카운터/지연 없음. SPEC 6.4 "일정 시간 후 자동 잠금", "인증 연속 실패 시 지연". `.env.example`의 `SESSION_TIMEOUT_MINUTES=15`/`MAX_AUTH_ATTEMPTS=5`는 읽는 코드 없음(1-3).
- **근거**: 자리 비움 시 볼트가 무기한 열려 있다. 5초 재마스킹은 '표시'만 가리는 것으로, `locked=false`인 한 클릭 한 번에 다시 노출된다. 실패 지연은 검증 로직이 생기는 VAULT-2에서 함께 필요(로컬 도구라 온라인 브루트포스는 아니지만, 어깨너머+물리 접근 시나리오는 SPEC 위협 모델 내).
- **개선안**: VAULT-2에 (a) 마지막 상호작용 기준 idle 타이머(기본 15분) → `lockNow()`, (b) 실패 n회 시 지수 지연을 포함. 값은 상수가 아니라 설정으로(1-3 정리와 연동).

### 3-3. [중] 잠금 해제 직후 5초간 볼트 전체 값이 일괄 자동 노출
- **위치**: `frontend/src/store/keylensStore.ts:192-206` (`startRemask` — `justUnlocked: true`), `frontend/src/components/vault/VaultRow.tsx:31` (`canSee = !locked && (justUnlocked || revealed)`)
- **근거**: 잠금 해제라는 행위만으로 사용자가 요청하지 않은 **모든 항목의 평문이 동시에 화면에 펼쳐진다**. "필요한 값만, 클릭 시, 4초간"이라는 항목별 reveal 원칙과 상충하고, 어깨너머 노출면(SPEC 6.1 방어 범위)을 스스로 넓힌다.
- **개선안**: 해제 후에도 기본은 마스킹 유지, 항목 클릭 시에만 개별 표시(이미 있는 `revealed` 경로로 일원화). `justUnlocked`는 "해제됨" 배너 표시에만 쓰고 값 노출 조건에서 제거 — `VaultRow.tsx:31`에서 `justUnlocked ||` 삭제 한 줄로 충분.

### 3-4. [하] 키 사용 감사 로그 부재 (누가/언제/무엇을 열람·복사·내보냈는지)
- **위치**: `frontend/src/types.ts:57-60` (`HistoryEntry` — 등록/회전만), `reveal`/`copy`/`envDownload`에 기록 없음
- **근거**: 단일 사용자 로컬 도구라 "누가"는 자명하지만, "언제 어떤 키를 복사/내보냈는지"는 유출 시점 역추적에 유용하다. SPEC이 약속한 항목은 아니므로 격하 판정.
- **개선안**: `history`에 `열람`/`복사`/`.env 내보내기` 이벤트를 추가하는 정도의 경량 구현(VAULT-1 이후, 로그 자체도 암호화 저장소 안에).

### 3-5. [하] 백엔드 API 무인증 — 현 구조에선 수용 가능, 단 유지 조건을 문서화할 것
- **위치**: `backend/app/main.py:20-30` (CORS 로컬 origin 한정), `main.py:73-78` (127.0.0.1 바인딩)
- **근거**: `/analyze`는 상태 비저장 분류기라 저장된 비밀에 접근할 수 없고, loopback 바인딩이라 원격 노출도 없다. 다만 요청 본문에 평문 키가 실려 다니므로, 향후 (a) 볼트 API가 백엔드에 생기거나 (b) 누군가 `--host 0.0.0.0`으로 띄우면 전제가 무너진다. 요청 크기 제한도 없다(거대 텍스트로 정규식 부하 — 로컬이라 하 판정).
- **개선안**: README 실행 안내에 "127.0.0.1 바인딩을 바꾸지 말 것" 명시. 볼트 API 도입 시점에 로컬 세션 토큰 등 최소 인증 추가. `AnalyzeRequest.text`에 상한(예: 64KB) 지정.

---

## 4. 암호화 구현 세부

### 4-1. [상] 암호화 구현이 0줄 — 설계(Argon2id + AES-256-GCM)는 적절하나 평가 대상이 없음
- **위치**: 부재 — `crypto.py`·`vault_repo.py` 없음(BACKLOG VAULT-1 체크리스트 전부 미완료), `backend/requirements.txt`에 `cryptography` 자체가 없음
- **근거**: 알고리즘 선택·IV/nonce 관리·패딩 오라클 등 사용자 점검 요청 항목은 **구현이 없어 평가 불가**. 설계 문서(SPEC 6.2)의 선택은 현행 권장 표준에 부합한다: Argon2id(KDF)·AES-256-GCM(AEAD, 패딩 없음 → 패딩 오라클 원천 배제)·항목별 고유 nonce·솔트 공개 저장. 설계대로만 구현되면 이 절의 우려는 대부분 해소된다.
- **개선안**: 구현 시 다음을 AC에 추가 — (a) nonce는 96-bit CSPRNG 랜덤, **키당 재사용 금지**(암호화 횟수가 많아질 경우 카운터 방식 고려), (b) Argon2id 파라미터 명시(예: m=64MiB, t=3, p=4 — 기기 사양 따라 조정), (c) 마스터 비밀번호 변경 시 재암호화 경로(SPEC에 이미 있음), (d) AAD에 항목 id·포맷 버전을 넣어 암호문 스왑 방지, (e) 금고 파일 포맷 버전 필드(SYNC-0 대비).

### 4-2. [통과] 하드코딩된 실제 키·시크릿 없음
- **근거**: 접두어 패턴 전수 스캔 결과 매치는 전부 명백한 더미다 — `seed.ts`의 `AIzaSyFAKE…`/`sk-proj-aAbB…`, 픽스처의 `sk-proj-Dummy…`, README의 `sk-proj-xxxx…`. `.gitignore`가 `.env`·`*.pem`·`*.key`·서비스계정 JSON·`/data/`·`*.sqlite`를 차단하고, 실키 이미지 사고를 커밋(`bb929f2`)으로 이미 교정한 이력도 있다. 더미 생성기(`docs/demo/generate.py`)는 KB 정규식에 맞춘 고정 상수를 쓴다. 위생 규율이 실제로 작동하고 있다.

### 4-3. [중] 마스킹이 과도하게 관대 — 접두어 없는 키의 앞 8자+뒤 4자 노출
- **위치**: `backend/app/masking.py:9-20` (`keep_front=8, keep_back=4`), 시드 데이터도 동일 규칙(`seed.ts:15-17` 카카오 키 마스킹)
- **근거**: `keep_front=8`의 취지는 "접두어 식별"인데, 접두어가 **공개 정보가 아닌** 키(카카오 32-hex 등)에도 일괄 적용된다. 32자 hex 키면 12/32자(37.5%)가 노출되고, 20자 값이면 12/20자(60%)까지 노출된다. 마스킹된 값은 **잠금 상태의 기본 표시**이므로(SPEC 6.4) 어깨너머·화면공유 시나리오에서 실질 노출면이다. 128-bit 키에서 48-bit가 새는 것 자체는 브루트포스를 성립시키지 않지만, "마스킹"이라는 이름값에 못 미친다.
- **개선안**: KB에 접두어 길이를 두고(`sk-proj-`=8, `AIza`=4, 없음=0), `keep_front = 접두어 길이 + 2` 정도로 제한. 접두어 없는 값은 앞 2~4자만.

### 4-4. [하] `mask()` 경계값: 7~11자 값은 절반 이상 노출
- **위치**: `backend/app/masking.py:16-20`
- **근거**: 전체 마스킹 조건이 `n <= keep_back + 2`(6자 이하)뿐이라, 예컨대 7자 값은 `front=1, back=4` → 5/7자 노출, 2자만 가려진다. 현행 KB 키들은 모두 20자 이상이라 당장 실해는 없지만, 짧은 시크릿(PIN·짧은 토큰)이 들어오는 순간 무너지는 경계다.
- **개선안**: `n < 12`면 전체 마스킹(`BULLET * n`). doctest에 짧은 값 케이스 추가.

---

## 5. 장애/예외 상황 대응

### 5-1. [중] 백엔드 장애 시 **가짜 샘플 결과**로 폴백 — 실패가 성공처럼 보임
- **위치**: `frontend/src/store/keylensStore.ts:392-399` (`catch` → `freshResults()` 목업), `keylensStore.ts:362-369` (입력 없음 시 샘플 폴백)
- **근거**: 백엔드가 죽으면 사용자가 방금 붙여넣은 **실제 입력과 무관한** 더미 5건이 결과 카드로 뜬다. 토스트("샘플 결과로 시연합니다")는 2.8초 후 사라지고, 카드들은 정상 결과와 동일한 UI로 저장까지 가능하다. 사용자는 (a) 자기 키가 분류됐다고 착각하거나 (b) 더미를 진짜로 알고 저장할 수 있다. "보안을 해치는 폴백"의 전형 — 장애를 조용히 성공으로 위장한다. (`apiError` 상태는 존재하나 결과 화면에 영구 표시되는지 불분명하며, 저장 차단은 없다.)
- **개선안**: (a) 폴백 결과 카드에 "샘플 데이터" 워터마크 배지 + `apiError` 배너를 결과 영역 상단에 **영구** 표시, (b) 샘플 폴백 상태에서는 저장/모두 저장 비활성화, (c) 데모 목업은 장애 폴백이 아니라 명시적 "샘플 체험" 버튼으로만 진입(이미 `attachSample` 경로가 있으므로 장애 폴백 쪽만 잘라내면 됨).

### 5-2. [통과에 가까움 / 하] 에러 메시지의 민감정보 노출 — 대체로 양호
- **근거**: `ApiError` 메시지는 일반화되어 있고(`client.ts:30-37`), OCR 실패 토스트도 값 미포함, 백엔드에 로깅 코드 없음(uvicorn 액세스 로그는 URL만 기록, 값은 POST 본문). 남는 소지: FastAPI 422 응답은 pydantic `input` 에코를 포함하나 `AnalyzeRequest`가 `Optional[str]` 2필드뿐이라 사실상 발생하지 않음. VAULT API 도입 시 예외 핸들러에서 에코 제거를 잊지 말 것.

### 5-3. [하] Stage1 값 절단이 조용히 일어남 — 잘린 키가 그대로 저장될 수 있음
- **위치**: `backend/app/classify/stage1.py:21-25` (`_ASSIGN_RE` — 값을 `[^"'\s#]+`로 캡처)
- **근거**: 값에 `#`이나 따옴표가 포함되면(일부 토큰 포맷·비밀번호형 시크릿) 그 지점에서 잘린 값이 후보가 되고, 사용자가 알아채지 못하면 **불완전한 키가 볼트에 저장**된다(무결성). OCR 이음매에는 "여기 확인" 표식 UX를 이미 만들었으므로 같은 문제의식이 여기만 빠져 있다.
- **개선안**: 절단이 발생한 경우(`m.end()`가 공백/EOL이 아닌 경우) `meta`에 `truncated: true`를 달아 프론트가 경고 표시. `.env` 규약상 따옴표 감싸기 해제도 함께 처리하면 정확도가 오른다.

### 5-4. [통과] 그 외 예외 경로
- OCR 실패 시 텍스트·URL만으로 계속 진행 + 안내 토스트(`keylensStore.ts:352-354`) — 적절.
- 분석 중 리셋 경합은 `if (!get().analyzing) return` 가드로 처리 — 적절.
- 지식베이스 스키마 위반 시 기동 실패(fail-closed, `knowledge.py:55-89`) — 적절. (단 "잘못된 YAML → 명확한 에러" 엣지 테스트는 BACKLOG에 미완로 남아 있음.)

---

## 6. 기타 (저장소 위생·부수 관찰)

### 6-1. [하] 미추적 프로토타입 폴더가 .gitignore에 없음
- **위치**: 루트 `KeyLens 자격증명 관리 도구/` (KeyLens.dc.html, support.js — Claude Design 프로토타입 export)
- **근거**: 현재 더미 값만 포함하나, `.gitignore`에 없어 `git add .` 한 번에 91KB HTML 원형이 통째로 커밋될 수 있다. 제출물 위생·저장소 크기 관점의 정리 대상.
- **개선안**: `.gitignore`에 추가하거나 `docs/prototype/`로 의도적으로 편입(SPDX 헤더 포함 여부도 그때 결정 — reuse lint 대상이 된다).

### 6-2. [하] 전역 paste 리스너가 입력 화면에서 클립보드 텍스트를 무조건 흡수
- **위치**: `frontend/src/App.tsx:19-47`
- **근거**: 입력 뷰에서는 포커스가 어디에 있든 붙여넣기 내용이 `textVal` 상태로 들어간다. 사용자가 다른 용도로 복사해 둔 무관한 비밀 텍스트가 의도치 않게 "분석 대상"으로 잡힐 수 있다. 낮은 위험이지만 시크릿을 다루는 도구답게 명시적 영역(붙여넣기 박스 포커스 시)으로 좁히는 편이 안전하다.

### 6-3. [메모] 분류 결과 카드(`ResultCard`)의 OCR 확인 블록은 값 전문(`r.full`)을 평문 표시
- 저장 전 "사용자가 방금 제공한 입력"의 확인 단계이므로 결함으로 보지 않는다. 다만 화면 공유 중 분석하는 시나리오를 한계 절에 언급해 둘 가치는 있다.

---

## 우선순위 요약 및 조치 현황 (2026-07-05 수정 적용)

즉시 조치 항목은 같은 날 적용했고, **핵심이던 VAULT-1/2(암호화 저장·인증 게이트)도 구현·검증 완료**했다
(2026-07-05: backend `crypto.py`/`vault_repo.py`/`vault_session.py`, pytest 66 통과 + 브라우저 E2E 검증).
남은 ⏳ 항목은 소규모 후속(감사 이력·회전 UX 등)이다.

| # | 심각도 | 한 줄 요약 | 상태 |
|---|---|---|---|
| 1-1 | 상 | "AES-256 암호화 완료" 등 허위 보안 문구 | ✅ 적용 — UI 문구 전면 교체 + 사이드바 데모 배너 + README 구현 상태 명시 |
| 3-1 | 상 | 아무 비밀번호나 잠금 해제 (인증 부재) | ✅ 적용 — VAULT-2: Argon2id 키 유도 + 검증기, 오답 401(브라우저 검증) |
| 2-1 | 상 | 평문 메모리 저장·영속성 없음 | ✅ 적용 — VAULT-1: AES-256-GCM 암호화 → SQLite 암호문만 |
| 2-2 | 상 | 잠금 상태에서 스크린샷으로 원문 열람 가능 | ✅ 적용 — 스크린샷을 값과 동일한 공개 조건(canSee)으로 가드 |
| 5-1 | 중 | 백엔드 장애 시 가짜 결과 폴백 | ✅ 적용 — 폴백 결과 저장 차단(save/saveAll), 경고 배너는 기존 유지 |
| 3-3 | 중 | 해제 직후 전체 값 5초 자동 노출 | ✅ 적용 — 일괄 노출 제거, 항목별 클릭 시 4초 표시로 일원화 |
| 2-4 | 중 | 클립보드 30초 삭제 허위 안내 | ✅ 적용 — 30초 후 검증-후-삭제 실제 구현(readText 미지원 브라우저는 삭제 생략) |
| 1-3 | 중 | `.env.example` 유령 설정 (`VITE_API_BASE_URL` 등) | ✅ 적용 — 실제 읽히는 `VITE_API_BASE`만 남기고 미래 설정은 주석 처리 |
| 1-2 | 중 | README 네이티브 Tesseract 오안내 | ✅ 적용 — tesseract.js(브라우저) 기준으로 요구사항·아키텍처 수정 |
| 1-4 | 중 | Kakao Native 키 프론트 매핑 누락 | ✅ 적용 — TYPE_MAP에 KAKAO_NATIVE_APP_KEY 추가 (근본 해법인 /knowledge 동적화는 INTEG-1 후속) |
| 2-3 | 중 | "회전 기록"이 값 교체가 아님 | ⏳ 미적용 — 값 교체 플로우 UX 재설계 필요 (S4) |
| 3-2 | 중 | 자동 잠금·실패 지연 부재 | ✅ 적용 — VAULT-2: 무활동 자동 잠금 + 연속 실패 백오프(429) |
| 4-3 | 중 | 마스킹이 앞 8자 일괄 노출 | ✅ 적용 — 비접두어 값(unknown·ambiguous·Stage2)은 keep_front=4로 축소 |
| 4-4 | 하 | 짧은 값(7~11자) 마스킹 누출 | ✅ 적용 — 12자 미만 전체 마스킹 |
| 3-5 | 하 | 백엔드 무인증·입력 상한 없음 | ✅ 부분 적용 — 요청 상한(text 100k/url 4k) + README에 127.0.0.1 유지 명시 |
| 6-1 | 하 | 미추적 프로토타입 폴더 | ✅ 적용 — .gitignore 추가 |
| 3-4 | 하 | 열람·복사 감사 이력 없음 | ⏳ VAULT-1 이후 |
| 5-3 | 하 | Stage1 값 절단이 조용히 발생 | ⏳ 미적용 — truncated 메타 + 카드 표식 후속 |
| 1-5 | 하 | SPEC "10종" AC 미충족 | ⏳ 미적용 — CORE-1 미결 결정 필요 |
| 6-2 | 하 | 전역 paste 리스너 | ⏳ 미적용 — 설계 판단 필요 |

**감사자 총평(2026-07-05 갱신)**: 위생(시크릿 커밋 방지·라이선스 규율)과 분류 엔진의 "단정 금지" 설계는 상급이다. 감사 시점의 최대 리스크였던 "허위 보안 표시 + 미구현 금고"는 **해소됐다** — 허위 문구를 실제 상태로 교체했고, **VAULT-1/2(Argon2id + AES-256-GCM 암호화 저장, 인증 게이트)를 설계(SPEC 6.2/6.4)대로 구현·브라우저 검증**했다. 이로써 프로젝트의 정체성("암호화해 나만 볼 수 있게 보관")이 코드로 성립한다. 남은 항목은 소규모 후속(열람 감사 이력, 회전 UX, Stage1 절단 표식 등)이다.
