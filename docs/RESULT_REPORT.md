<!--
SPDX-FileCopyrightText: 2026 [Your Name]
SPDX-License-Identifier: MIT
-->
# KeyLens — 결과보고서

> **한 줄 정의**: 스크린샷·URL을 던지면 "이건 Notion API 키, 이건 database ID"처럼 **출처의 맥락으로 정체를 가려**
> 공식 환경변수명에 매핑하고, 마스터 비밀번호로 **암호화해 나만 볼 수 있게** 보관하는 개인 개발자용 자격증명 관리 도구.
>
> 오픈소스 개발자대회 자유과제(보안) · 솔로 · MIT.

---

## 1. 문제 정의

API 연동을 하다 보면 키·토큰·ID가 끝없이 쌓인다. 이것들은 보통 메모장이나 이름 없는 텍스트 파일에 흩어지고, 두 가지 문제를 낳는다.

1. **이게 무슨 키인지 모른다.** — Notion의 `database_id`·`data_source_id`·`page_id`는 **전부 같은 32자리 UUID**라 값만 봐서는 구분이 불가능하다. Kakao는 REST/JavaScript/Admin/Native 키가 한 화면에 비슷한 모습으로 나열되고, GCP는 API 키·서비스 계정 JSON·OAuth 클라이언트가 뒤섞인다.
2. **안전하게 보관할 곳이 없다.** — 평문 메모는 유출에 취약하고, 정작 필요할 때 어떤 값이 무엇이었는지 다시 헷갈린다.

**기존 시크릿 스캐너(정규식 기반)는 정확히 1번에서 손든다.** 값의 형식이 같으면 "여러 후보 가능"이라며 구분을 포기한다.

---

## 2. 차별점 — 값이 아니라 **출처의 맥락**으로 분류한다 (프로젝트의 심장)

KeyLens는 값 자체가 아니라 **값이 있던 자리의 맥락**(스크린샷 속 라벨, URL 구조)을 분류 신호로 쓴다.

- **2단계 분류 엔진**
  - **Stage 1 (값 기반)**: 접두어가 명확한 키(`sk-` OpenAI, `AIza` Google, `secret_`/`ntn_` Notion, `<32hex>.<24base62>` Ollama)를 정규식으로 즉시 식별.
  - **Stage 2 (맥락 기반) ★차별점**: 값만으론 애매한 키(UUID·32hex)를 **주변 라벨**("Database ID", "REST API 키")과 **URL 구조**(`notion.so/…?v=` 앞 세그먼트 = database_id)로 가려낸다.
- **단정 금지가 원칙**: 라벨과 URL 신호가 충돌하면 임의로 단정하지 않고 **"확인 필요(conflict)"** 로 두 후보를 제시해 사용자가 고르게 한다. — 잘못된 확신보다 정직한 미결이 낫다.

> **한 문장 반박**: "기존 도구는 `3f9a1c2e…` 를 보고 '32자리 hex'라고만 말하고 멈춘다. KeyLens는 그 값 **위에 적힌 'Database ID'** 를 읽어 `NOTION_DATABASE_ID` 라고 답한다."

**실측**: 실제 Ollama 콘솔 스크린샷 → 브라우저 OCR → `OLLAMA_API_KEY`(high) 분류까지 엔드투엔드 확인. 노션 DB URL은 `database_id`, page URL은 `page_id`로 구분.

---

## 3. 아키텍처 — 로컬 우선(local-first), 외부 서버 없음

```
[ React + TypeScript 프론트엔드 (:5173) ]      ←→   [ FastAPI 로컬 백엔드 (:8003) ]
  ├─ OCR: tesseract.js (브라우저 WASM)                ├─ 분류 엔진 (Stage1 값 / Stage2 맥락)
  │    └ 이미지가 기기를 떠나지 않음                     ├─ 지식베이스 로더 (knowledge/*.yaml)
  ├─ 상태: Zustand                                     └─ 암호화 저장소
  └─ UI: 입력·결과카드·보관함·잠금                          ├ crypto.py  (Argon2id + AES-256-GCM)
                                                        ├ vault_repo.py (SQLite — 암호문만)
                                                        └ vault_session.py (세션 인증·자동잠금)
```

- **로컬 우선**: 외부 서버·클라우드가 없다. 모든 데이터는 사용자 기기에만 존재하며 네트워크로 전송되지 않는다(백엔드도 `127.0.0.1` 전용).
- **OCR을 브라우저로**: 이미지가 백엔드로도 나가지 않는다. WASM·언어데이터는 로컬 벤더링(런타임 CDN 미사용) — 오프라인·재현성.
- **분류와 OCR 분리**: 차별점(맥락 분류)을 OCR 안정성에 인질로 잡히지 않게, text·URL 신호로 먼저 완성하고 OCR은 "이미지→텍스트+라벨" 입력 경로로 붙였다.

---

## 4. 핵심 기능

| 기능 | 설명 |
|---|---|
| **다양한 입력** | 콘솔 스크린샷(드래그·붙여넣기), URL, 텍스트 |
| **2단계 분류 + 공식 변수명 매핑** | 분류 결과를 `NOTION_DATABASE_ID`·`KAKAO_REST_API_KEY` 등 표준 환경변수명으로 정리 |
| **값 정밀 재인식** | OCR 값 오독을 값 영역만 PSM+charset로 2차 인식해 교정(길이 가드로 퇴행 방지). 불확실 지점은 빨간 표식+토스트로 안내 |
| **암호화 금고** | 마스터 비밀번호 → Argon2id 키 → AES-256-GCM으로 값 암호화, SQLite에 암호문만 저장 |
| **인증 게이트** | 값 조회·복사·내보내기 전 인증. 무활동 자동 잠금, 연속 실패 백오프 지연 |
| **감사 이력** | 등록·열람·복사·내보내기·키 교체를 기록(값 없이) — 유출 시점 역추적 |
| **키 회전** | 서비스에서 재발급한 새 값으로 교체(재암호화, 옛 값 폐기) |
| **`.env` 내보내기** | 선택 키를 표준 변수명으로 내보내기(인증 후, 평문 노출 경고) |
| **확장 가능한 지식베이스** | 서비스 정의가 YAML — 파일 하나 추가로 새 서비스 지원(코드 수정 0) |

---

## 5. 보안 설계

- **키 유도**: 마스터 비밀번호 → **Argon2id**(메모리-하드 KDF)로 32B 키 유도. 솔트·파라미터만 저장하고 **유도된 키는 메모리에만** 존재, 잠금 시 폐기.
- **암호화**: 항목별 **AES-256-GCM**(인증 암호화), 매 암호화마다 고유 nonce. GCM 태그로 변조·오답 비밀번호를 거부. 항목을 `official_name`에 AAD로 묶어 DB 라벨 스왑 변조까지 탐지.
- **저장**: SQLite에는 **암호문·nonce·메타데이터만** — 평문 값 컬럼 자체가 없다. 파일을 직접 열어도 평문 미노출(바이트 스캔 테스트로 검증).
- **접근 통제**: 조회·복사·내보내기 전 인증 필수, 무활동 자동 잠금(기본 5분, 설정 가능), 연속 실패 시 지수 백오프.
- **정직한 산출물**: 자체 보안 감사(SECURITY_REVIEW.md)를 수행하고, "구현 예정"을 "완료"로 표시하는 등의 **허위 보안 표시를 전부 제거**했다. UI·문서가 실제 구현 상태와 일치한다.

**방어 범위 밖(한계, 정직하게 명시)**: 이미 장악된 호스트(키로거·메모리 덤프), 약한 마스터 비밀번호. KeyLens는 **분실·도난 기기의 디스크 접근, 파일 유출, 저장소 직접 열람**으로부터 보호한다.

---

## 6. 오픈소스 전략

- **확장이 데이터로 끝난다**: 서비스별 키 종류·라벨·URL 패턴·변수명을 코드가 아니라 `knowledge/*.yaml`로 선언. 새 서비스는 **YAML 한 개 추가 = PR 한 건**(코드 수정 0). 실제로 Ollama를 이 방식으로 추가했다. → 기여 진입장벽이 낮아 커뮤니티 확장에 유리.
- **허용적 라이선스 규율**: 전체가 MIT + 허용적 의존성만(Apache-2.0/BSD/ISC). **카피레프트·MPL 금지**를 상시 지켰다 — `certifi`(MPL, httpx 경유)·`lightningcss`(MPL, 빌드 CSS 도구)를 감지·제거/재분류. 자체 `reuse lint` 통과, SBOM([docs/SBOM.md](./SBOM.md)) 작성.
- **참고 규율**: 다른 도구의 코드/패턴을 그대로 포팅하지 않고 공식 문서 기준으로 직접 재구현. AGPL(TruffleHog) 포팅 금지, MIT(Gitleaks) 참고 시 출처 기록.

---

## 7. 기술 스택 (전부 permissive)

| 영역 | 선택 | 라이선스 |
|---|---|---|
| 백엔드 | FastAPI · uvicorn · pydantic · PyYAML | MIT / BSD-3 |
| 암호화 | cryptography (Argon2id + AES-256-GCM) | Apache-2.0 OR BSD-3 |
| 프론트 | React · TypeScript · Zustand · Tailwind · Radix UI | MIT / ISC |
| OCR | tesseract.js (브라우저 WASM) + tessdata(eng·kor) | Apache-2.0 |
| 저장소 | SQLite | Public Domain |

전체 의존성·전이·버전은 [docs/SBOM.md](./SBOM.md) 참고. **런타임 트리 카피레프트 0건.**

---

## 8. 한계 (정직하게)

- **OCR 값 정확도**: 고엔트로피 시크릿의 완벽한 OCR은 본질적으로 불안정하다(예: 숫자 `1`↔글자 `i`, 한글 단일글자 "키"→"7|"). 그래서 **값은 복붙을 전제**로 두고, OCR은 "무슨 키인지 분류"에 집중하며 불확실 지점을 사용자에게 표식으로 알린다. 전역 이미지 전처리는 실험 결과 오히려 퇴행이라 채택하지 않았다.
- **검출 범위**: 값 기반 자동 검출은 지식베이스 등록 서비스 기준(현재 5종). GitHub/AWS/Slack 등 추가 접두어는 검출 전용 규칙으로 확장 가능(설계됨). 미등록 서비스 키는 오식별 없이 `unknown` 처리.
- **미구현(의도적 stretch/로드맵)**: 키 유효성 실호출 체크(TRUST-1), 만료 임박 알림(TRUST-2), 암호화 금고 파일 내보내기/가져오기(SYNC-0), Google Drive 제로널리지 동기화(SYNC-1)는 로드맵.

---

## 9. 재현성

```bash
# 설치 (처음 한 번)
cd backend && python -m venv .venv && .venv\Scripts\activate && pip install -r requirements.txt && cd ..
cd frontend && npm ci && cd ..
# 실행 (한 번에)
node scripts/dev.mjs         # 백엔드 :8003 + 프론트 :5173 동시 기동
```

- 의존성 버전 고정(requirements.txt / package-lock.json), 로컬 자산 자동 벤더링.
- **검증**: 백엔드 pytest 80개 · 프론트 vitest + build · **브라우저 E2E**(설정→분석→암호화 저장→조회→잠금→인증) 통과.
- 데모용 더미 스크린샷(`docs/demo/*.png`)으로 즉시 체험 가능.

---

## 10. 개발 과정 (AI 코딩 보조)

본 프로젝트는 Claude Code 등 AI 코딩 보조를 활용해 개발했다. 활용 모델·비율·직접 이해·설명 가능 범위는 **붙임2(AI 모델 활용 명세서)** 참고. OCR 엔진(Tesseract)의 활용 유형·라이선스도 붙임2에 명시했다.
